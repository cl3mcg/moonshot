<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Moonshot | Home</title>
    <%- include("partials/commonStyles.ejs") %>
    <%- include("partials/commonFonts.ejs") %>
</head>
<body>
    <%- include("partials/navbar.ejs") %>

    <div class="row mt-5">
        <div class="col col-12">
            <h1 class="text-center">Tender pre-advise</h1>
            <p class="lead ms-xs-2 ms-sm-5">Find below an organized schedule of the preadvised tenders</p>
        </div>
    </div>
    <div class="row mt-3">
        <div class="col col-12 col-lg-3 col-xl-2 col-xxl-1 ms-lg-2 ms-xl-5 mb-5">
            <div class="rounded-3 border py-3 sticky-lg-top">
                <nav class="nav flex-column">
                    <h5 class="text-center">Display options</h5>
                    <button class="btn btn-outline-light btn-block btn-sm border m-1" id="displayDateBtn"><span>By date</span></button>
                    <button class="btn btn-outline-light btn-block btn-sm border m-1" id="displayOfficeBtn"><span>By tender desk</span></button>
                    <button class="btn btn-outline-light btn-block btn-sm border m-1" id="displayTableBtn"><span>Compact table</span></button>
                </nav>
            </div>
        </div>
        <div id="displayDate" class="col me-lg-2 me-xl-5 mb-5">
            <div class="rounded-3 border pt-3">
                <div class="container mb-4">
                    <h2>This month</h2>
                    <h3><%= monthsData[today.getMonth() + 1]%>-<%= today.getFullYear() %></h3>
                    <div class="row">
                        <% if (!preadvised_inM || preadvised_inM.length === 0) {%>
                            <div class="col col-12 col-sm-4 col-xl-3 mt-2">
                                <div class="card h-100 text-muted">
                                    <div class="card-body">
                                    <h5 class="card-title"><i class="bi bi-file-earmark-excel"></i> Empty...</h5>
                                    <p class="card-text">No preadvised tenders are matching this time frame.</p>
                                    </div>
                                </div>
                            </div>
                            <% } else { %>  
                        <% for (let entry of preadvised_inM) { %>
                            <div class="col col-12 col-sm-4 col-xl-3">
                                <div class="card h-100 <% if ((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) <= 10) { %> 
                                border-danger
                                <% } %>">
                                    <% if ((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) <= 10) { %> 
                                        <span class="position-absolute top-0 start-100 translate-middle badge bg-danger fs-6"><i class="bi bi-alarm"></i></span>
                                    <% } %>
                                    <h5 class="card-header text-truncate"><%= entry.companyName %></h5>
                                        <div class="card-body">
                                            <h6 class="card-title mb-0">Launch expected: <% if(entry.expectedReceiveDate.getDate() < 10) { %><%=`0${entry.expectedReceiveDate.getDate()}`%><% } else { %><%= `${entry.expectedReceiveDate.getDate()}` %><% } %>-<%= `${monthsData[entry.expectedReceiveDate.getMonth() + 1]}-${entry.expectedReceiveDate.getFullYear()}`%></h6>
                                            <p class="card-text mt-0">From <% for (let country of countriesData) { if (country.cca2 === entry.countryLocation) { %><%= country.name.common %> <%= country.flag %><% } %><% } %></p>
                                            <p class="card-text mb-0"><% if (entry.history.includes("historyNone")) { %><%="No history background"%><% } else if (entry.history.includes("historyAirOcean") || entry.history.includes("historyAir&Ocean")) { %><%="Existing Rhenus Air & Ocean history"%><% } else { %><%="Existing Rhenus Group history"%><% } %></p>
                                            <% if (entry.history.includes("historyAirOcean") || entry.history.includes("historyAir&Ocean")) {%><p class="card-text mt-0"><i><%= entry.existingCustomerSegment %></i></p><%}%> 
                                            <% if (entry.airFreightVolume) {%><p class="card-text mb-0"><%=`Air vol. ${parseInt(entry.airFreightVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} Tons`%></p><%} if (entry.seaFreightFCLVolume) {%><p class="card-text mb-0"><%=`FCL vol. ${parseInt(entry.seaFreightFCLVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} TEUs`%></p><%} if (entry.seaFreightLCLVolume) {%><p class="card-text mb-0"><%=`LCL vol. ${parseInt(entry.seaFreightLCLVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} CBMs`%></p><%} if (entry.railFreightVolume) {%><p class="card-text mb-0"><%=`Rail vol. ${parseInt(entry.railFreightVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} TEUs`%></p><% } %>
                                            <a href="/moonshot/preadvised/<%= entry._id %> " class="card-link btn btn-light btn-sm border mt-3">Details</a>
                                        </div>
                                </div>
                            </div>
                        <% } %>
                        <% } %>
                    </div>
                </div>
                <div class="container mb-4">
                    <h2>Within the next 3 months</h2>
                    <h3>from <%= monthsData[next30days.getMonth() + 1]%>-<%= next30days.getFullYear() %> to <%= monthsData[next90days.getMonth() + 1]%>-<%= next90days.getFullYear() %></h3>
                    <div class="row">
                        <% if (!preadvised_inQ || preadvised_inQ.length === 0) {%>
                            <div class="col col-12 col-sm-4 col-xl-3 mt-2">
                                <div class="card h-100 text-muted">
                                    <div class="card-body">
                                        <h5 class="card-title"><i class="bi bi-file-earmark-excel"></i> Empty...</h5>
                                        <p class="card-text">No preadvised tenders are matching this time frame.</p>
                                    </div>
                                </div>
                            </div>
                            <% } else { %>  
                        <% for (let entry of preadvised_inQ) { %>
                            <div class="col col-12 col-sm-4 col-xl-3">
                                <div class="card h-100 <% if ((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) <= 10) { %> 
                                border-danger
                                <% } %>">
                                    <% if ((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) <= 10) { %> 
                                        <span class="position-absolute top-0 start-100 translate-middle adge bg-danger fs-6"><i class="bi bi-alarm"></i></span>
                                    <% } %>
                                    <h5 class="card-header text-truncate"><%= entry.companyName %></h5>
                                        <div class="card-body">
                                            <h6 class="card-title mb-0">Launch expected: <% if(entry.expectedReceiveDate.getDate() < 10) { %><%=`0${entry.expectedReceiveDate.getDate()}`%><% } else { %><%= `${entry.expectedReceiveDate.getDate()}` %><% } %>-<%= `${monthsData[entry.expectedReceiveDate.getMonth() + 1]}-${entry.expectedReceiveDate.getFullYear()}`%></h6>
                                            <p class="card-text mt-0">From <% for (let country of countriesData) { if (country.cca2 === entry.countryLocation) { %><%= country.name.common %> <%= country.flag %><% } %><% } %></p>
                                            <p class="card-text mb-0"><% if (entry.history.includes("historyNone")) { %><%="No history background"%><% } else if (entry.history.includes("historyAirOcean") || entry.history.includes("historyAir&Ocean")) { %><%="Existing Rhenus Air & Ocean history"%><% } else { %><%="Existing Rhenus Group history"%><% } %></p>
                                            <% if (entry.history.includes("historyAirOcean") || entry.history.includes("historyAir&Ocean")) {%><p class="card-text mt-0"><i><%= entry.existingCustomerSegment %></i></p><%}%> 
                                            <% if (entry.airFreightVolume) {%><p class="card-text mb-0"><%=`Air vol. ${parseInt(entry.airFreightVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} Tons`%></p><%} else if (entry.seaFreightFCLVolume) {%><p class="card-text mb-0"><%=`FCL vol. ${parseInt(entry.seaFreightFCLVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} TEUs`%></p><%} else if (entry.seaFreightLCLVolume) {%><p class="card-text mb-0"><%=`LCL vol. ${parseInt(entry.seaFreightLCLVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} CBMs`%></p><%} else if (entry.railFreightVolume) {%><p class="card-text mb-0"><%=`Rail vol. ${parseInt(entry.railFreightVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} TEUs`%></p><% } %>
                                            <a href="/moonshot/preadvised/<%= entry._id %> " class="card-link btn btn-light btn-sm border mt-3">Details</a>
                                        </div>
                                </div>
                            </div>
                        <% } %>
                        <% } %>
                    </div>
                </div>
                <div class="container mb-5">
                    <h2>In the future</h2>
                    <h3>after <%= monthsData[next90days.getMonth() + 1]%>-<%= next90days.getFullYear() %></h3>
                    <div class="row">
                        <% if (!preadvised_inY || preadvised_inY.length === 0) {%>
                            <div class="col col-12 col-sm-4 col-xl-3 mt-2">
                                <div class="card h-100 text-muted">
                                    <div class="card-body">
                                        <h5 class="card-title"><i class="bi bi-file-earmark-excel"></i> Empty...</h5>
                                        <p class="card-text">No preadvised tenders are matching this time frame.</p>
                                    </div>
                                </div>
                            </div>
                            <% } else { %>  
                        <% for (let entry of preadvised_inY) { %>
                            <div class="col col-12 col-sm-4 col-xl-3">
                                <div class="card h-100 <% if ((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) <= 10) { %> 
                                border-danger
                                <% } %>">
                                    <% if ((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) <= 10) { %> 
                                        <span class="position-absolute top-0 start-100 translate-middle adge bg-danger fs-6"><i class="bi bi-alarm"></i></span>
                                    <% } %>
                                    <h5 class="card-header text-truncate"><%= entry.companyName %></h5>
                                        <div class="card-body">
                                            <h6 class="card-title mb-0">Launch expected: <% if(entry.expectedReceiveDate.getDate() < 10) { %><%=`0${entry.expectedReceiveDate.getDate()}`%><% } else { %><%= `${entry.expectedReceiveDate.getDate()}` %><% } %>-<%= `${monthsData[entry.expectedReceiveDate.getMonth() + 1]}-${entry.expectedReceiveDate.getFullYear()}`%></h6>
                                            <p class="card-text mt-0">From <% for (let country of countriesData) { if (country.cca2 === entry.countryLocation) { %><%= country.name.common %> <%= country.flag %><% } %><% } %></p>
                                            <p class="card-text mb-0"><% if (entry.history.includes("historyNone")) { %><%="No history background"%><% } else if (entry.history.includes("historyAirOcean") || entry.history.includes("historyAir&Ocean")) { %><%="Existing Rhenus Air & Ocean history"%><% } else { %><%="Existing Rhenus Group history"%><% } %></p>
                                            <% if (entry.history.includes("historyAirOcean") || entry.history.includes("historyAir&Ocean")) {%><p class="card-text mt-0"><i><%= entry.existingCustomerSegment %></i></p><%}%> 
                                            <% if (entry.airFreightVolume) {%><p class="card-text mb-0"><%=`Air vol. ${parseInt(entry.airFreightVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} Tons`%></p><%} else if (entry.seaFreightFCLVolume) {%><p class="card-text mb-0"><%=`FCL vol. ${parseInt(entry.seaFreightFCLVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} TEUs`%></p><%} else if (entry.seaFreightLCLVolume) {%><p class="card-text mb-0"><%=`LCL vol. ${parseInt(entry.seaFreightLCLVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} CBMs`%></p><%} else if (entry.railFreightVolume) {%><p class="card-text mb-0"><%=`Rail vol. ${parseInt(entry.railFreightVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} TEUs`%></p><% } %>
                                            <a href="/moonshot/preadvised/<%= entry._id %> " class="card-link btn btn-light btn-sm border mt-3">Details</a>
                                        </div>
                                </div>
                            </div>
                        <% } %>
                        <% } %>
                    </div>
                </div>
                <div class="container mb-4">
                    <h2 class="text-muted">Past preadvised</h2>
                    <h3 class="text-muted">Before <%= monthsData[today.getMonth() + 1]%>-<%= today.getFullYear() %></h3>
                    <div class="row">
                        <% if (!preadvised_past || preadvised_past.length === 0) {%>
                            <div class="col col-12 col-sm-4 col-xl-3 mt-2">
                                <div class="card h-100 text-muted">
                                    <div class="card-body">
                                        <h5 class="card-title"><i class="bi bi-file-earmark-excel"></i> Empty...</h5>
                                        <p class="card-text">No preadvised tenders are matching this time frame.</p>
                                    </div>
                                </div>
                            </div>
                            <% } else { %>  
                        <% for (let entry of preadvised_past) { %>
                            <div class="col col-12 col-sm-4 col-xl-3">
                                <div class="card h-100 <% if ((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) >= -10) { %> 
                                border-secondary
                                <% } %>">
                                    <% if ((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) >= -10) { %> 
                                        <span class="position-absolute top-0 start-100 translate-middle badge bg-secondary fs-6"><i class="bi bi-alarm"></i></span>
                                    <% } %>
                                    <h5 class="card-header text-truncate"><%= entry.companyName %></h5>
                                        <div class="card-body">
                                            <h6 class="card-title mb-0">Launch expected: <% if(entry.expectedReceiveDate.getDate() < 10) { %><%=`0${entry.expectedReceiveDate.getDate()}`%><% } else { %><%= `${entry.expectedReceiveDate.getDate()}` %><% } %>-<%= `${monthsData[entry.expectedReceiveDate.getMonth() + 1]}-${entry.expectedReceiveDate.getFullYear()}`%></h6>
                                            <p class="card-text mt-0">From <% for (let country of countriesData) { if (country.cca2 === entry.countryLocation) { %><%= country.name.common %> <%= country.flag %><% } %><% } %></p>
                                            <p class="card-text mb-0"><% if (entry.history.includes("historyNone")) { %><%="No history background"%><% } else if (entry.history.includes("historyAirOcean") || entry.history.includes("historyAir&Ocean")) { %><%="Existing Rhenus Air & Ocean history"%><% } else { %><%="Existing Rhenus Group history"%><% } %></p>
                                            <% if (entry.history.includes("historyAirOcean") || entry.history.includes("historyAir&Ocean")) {%><p class="card-text mt-0"><i><%= entry.existingCustomerSegment %></i></p><%}%> 
                                            <% if (entry.airFreightVolume) {%><p class="card-text mb-0"><%=`Air vol. ${parseInt(entry.airFreightVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} Tons`%></p><%} else if (entry.seaFreightFCLVolume) {%><p class="card-text mb-0"><%=`FCL vol. ${parseInt(entry.seaFreightFCLVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} TEUs`%></p><%} else if (entry.seaFreightLCLVolume) {%><p class="card-text mb-0"><%=`LCL vol. ${parseInt(entry.seaFreightLCLVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} CBMs`%></p><%} else if (entry.railFreightVolume) {%><p class="card-text mb-0"><%=`Rail vol. ${parseInt(entry.railFreightVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} TEUs`%></p><% } %>
                                            <a href="/moonshot/preadvised/<%= entry._id %> " class="card-link btn btn-light btn-sm border mt-3">Details</a>
                                        </div>
                                </div>
                            </div>
                        <% } %>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
        <div id="displayOffice" class="col col-10 me-lg-2 me-xl-5 mb-5">
            <div class="rounded-3 border pt-3">
                <div class="container mb-4">
                    <div class="accordion" id="accordionPanelsStayOpenExample">
                        <div class="accordion-item">
                            <h2 class="accordion-header" id="panelsStayOpen-headingOne">
                            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#panelsStayOpen-collapseOne" aria-expanded="false" aria-controls="panelsStayOpen-collapseOne">
                                AM tender desk
                            </button>
                            </h2>
                            <div id="panelsStayOpen-collapseOne" class="accordion-collapse collapse" aria-labelledby="panelsStayOpen-headingOne">
                            <div class="accordion-body">
                                <div class="row">
                                    <% if (!preadvised_inAM || preadvised_inAM === 0) { %>
                                            <div class="col col-12 col-sm-4 col-xl-3 mt-2">
                                                <div class="card h-100 text-muted">
                                                    <div class="card-body">
                                                        <h5 class="card-title"><i class="bi bi-file-earmark-excel"></i> Empty...</h5>
                                                        <p class="card-text">No preadvised tenders are matching this tender desk.</p>
                                                    </div>
                                                </div>
                                            </div>
                                        <% } else { %> 
                                        <% for (entry of preadvised_inAM) { %>
                                            <div class="col col-12 col-sm-4 col-xl-3">
                                                <div class="card h-100 <% if (((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) < 0) && ((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) >= -10)) { %> 
                                                border-secondary
                                                <% } else if (((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) >= 0) && (new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) <= 10) { %> 
                                                    border-danger
                                                    <% } %>">
                                                    <% if ((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) <= 0 && (new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24 >= -10)) { %> 
                                                        <span class="position-absolute top-0 start-100 translate-middle badge bg-secondary fs-6"><i class="bi bi-alarm"></i></span>
                                                    <% } else if ((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) <= 10) { %> 
                                                        <span class="position-absolute top-0 start-100 translate-middle badge bg-danger fs-6"><i class="bi bi-alarm"></i></span>
                                                    <% } %>
                                                    <h5 class="card-header text-truncate"><%= entry.companyName %></h5>
                                                        <div class="card-body">
                                                            <h6 class="card-title mb-0">Launch expected: <% if(entry.expectedReceiveDate.getDate() < 10) { %><%=`0${entry.expectedReceiveDate.getDate()}`%><% } else { %><%= `${entry.expectedReceiveDate.getDate()}` %><% } %>-<%= `${monthsData[entry.expectedReceiveDate.getMonth() + 1]}-${entry.expectedReceiveDate.getFullYear()}`%></h6>
                                                            <p class="card-text mt-0">From <% for (let country of countriesData) { if (country.cca2 === entry.countryLocation) { %><%= country.name.common %> <%= country.flag %><% } %><% } %></p>
                                                            <p class="card-text mb-0"><% if (entry.history.includes("historyNone")) { %><%="No history background"%><% } else if (entry.history.includes("historyAirOcean") || entry.history.includes("historyAir&Ocean")) { %><%="Existing Rhenus Air & Ocean history"%><% } else { %><%="Existing Rhenus Group history"%><% } %></p>
                                                            <% if (entry.history.includes("historyAirOcean") || entry.history.includes("historyAir&Ocean")) {%><p class="card-text mt-0"><i><%= entry.existingCustomerSegment %></i></p><%}%> 
                                                            <% if (entry.airFreightVolume) {%><p class="card-text mb-0"><%=`Air vol. ${parseInt(entry.airFreightVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} Tons`%></p><%} else if (entry.seaFreightFCLVolume) {%><p class="card-text mb-0"><%=`FCL vol. ${parseInt(entry.seaFreightFCLVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} TEUs`%></p><%} else if (entry.seaFreightLCLVolume) {%><p class="card-text mb-0"><%=`LCL vol. ${parseInt(entry.seaFreightLCLVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} CBMs`%></p><%} else if (entry.railFreightVolume) {%><p class="card-text mb-0"><%=`Rail vol. ${parseInt(entry.railFreightVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} TEUs`%></p><% } %>
                                                            <a href="/moonshot/preadvised/<%= entry._id %> " class="card-link btn btn-light btn-sm border mt-3">Details</a>
                                                        </div>
                                                </div>
                                            </div>
                                        <% } %>
                                    <% } %> 
                                </div>
                            </div>
                            </div>
                        </div>
                        <div class="accordion-item">
                        <h2 class="accordion-header" id="panelsStayOpen-headingTwo">
                            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#panelsStayOpen-collapseTwo" aria-expanded="false" aria-controls="panelsStayOpen-collapseTwo">
                                AP tender desk
                            </button>
                        </h2>
                        <div id="panelsStayOpen-collapseTwo" class="accordion-collapse collapse" aria-labelledby="panelsStayOpen-headingTwo">
                            <div class="accordion-body">
                                <div class="row">
                                    <% if (!preadvised_inAP || preadvised_inAP === 0) { %>
                                            <div class="col col-12 col-sm-4 col-xl-3 mt-2">
                                                <div class="card h-100 text-muted">
                                                    <div class="card-body">
                                                        <h5 class="card-title"><i class="bi bi-file-earmark-excel"></i> Empty...</h5>
                                                        <p class="card-text">No preadvised tenders are matching this tender desk.</p>
                                                    </div>
                                                </div>
                                            </div>
                                        <% } else { %> 
                                        <% for (entry of preadvised_inAP) { %>
                                            <div class="col col-12 col-sm-4 col-xl-3">
                                                <div class="card h-100 <% if (((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) < 0) && ((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) >= -10)) { %> 
                                                border-secondary
                                                <% } else if (((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) >= 0) && (new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) <= 10) { %> 
                                                    border-danger
                                                    <% } %>">
                                                    <% if ((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) <= 0 && (new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24 >= -10)) { %> 
                                                        <span class="position-absolute top-0 start-100 translate-middle badge bg-secondary fs-6"><i class="bi bi-alarm"></i></span>
                                                    <% } else if ((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) <= 10) { %> 
                                                        <span class="position-absolute top-0 start-100 translate-middle badge bg-danger fs-6"><i class="bi bi-alarm"></i></span>
                                                    <% } %>
                                                    <h5 class="card-header text-truncate"><%= entry.companyName %></h5>
                                                        <div class="card-body">
                                                            <h6 class="card-title mb-0">Launch expected: <% if(entry.expectedReceiveDate.getDate() < 10) { %><%=`0${entry.expectedReceiveDate.getDate()}`%><% } else { %><%= `${entry.expectedReceiveDate.getDate()}` %><% } %>-<%= `${monthsData[entry.expectedReceiveDate.getMonth() + 1]}-${entry.expectedReceiveDate.getFullYear()}`%></h6>
                                                            <p class="card-text mt-0">From <% for (let country of countriesData) { if (country.cca2 === entry.countryLocation) { %><%= country.name.common %> <%= country.flag %><% } %><% } %></p>
                                                            <p class="card-text mb-0"><% if (entry.history.includes("historyNone")) { %><%="No history background"%><% } else if (entry.history.includes("historyAirOcean") || entry.history.includes("historyAir&Ocean")) { %><%="Existing Rhenus Air & Ocean history"%><% } else { %><%="Existing Rhenus Group history"%><% } %></p>
                                                            <% if (entry.history.includes("historyAirOcean") || entry.history.includes("historyAir&Ocean")) {%><p class="card-text mt-0"><i><%= entry.existingCustomerSegment %></i></p><%}%> 
                                                            <% if (entry.airFreightVolume) {%><p class="card-text mb-0"><%=`Air vol. ${parseInt(entry.airFreightVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} Tons`%></p><%} else if (entry.seaFreightFCLVolume) {%><p class="card-text mb-0"><%=`FCL vol. ${parseInt(entry.seaFreightFCLVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} TEUs`%></p><%} else if (entry.seaFreightLCLVolume) {%><p class="card-text mb-0"><%=`LCL vol. ${parseInt(entry.seaFreightLCLVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} CBMs`%></p><%} else if (entry.railFreightVolume) {%><p class="card-text mb-0"><%=`Rail vol. ${parseInt(entry.railFreightVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} TEUs`%></p><% } %>
                                                            <a href="/moonshot/preadvised/<%= entry._id %> " class="card-link btn btn-light btn-sm border mt-3">Details</a>
                                                        </div>
                                                </div>
                                            </div>
                                        <% } %>
                                    <% } %> 
                                </div>
                            </div>
                        </div>
                        </div>
                        <div class="accordion-item">
                        <h2 class="accordion-header" id="panelsStayOpen-headingThree">
                            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#panelsStayOpen-collapseThree" aria-expanded="false" aria-controls="panelsStayOpen-collapseThree">
                            EU tender desk
                            </button>
                        </h2>
                        <div id="panelsStayOpen-collapseThree" class="accordion-collapse collapse" aria-labelledby="panelsStayOpen-headingThree">
                            <div class="accordion-body">
                                <div class="row">
                                    <% if (!preadvised_inEU || preadvised_inEU === 0) { %>
                                            <div class="col col-12 col-sm-4 col-xl-3 mt-2">
                                                <div class="card h-100 text-muted">
                                                    <div class="card-body">
                                                        <h5 class="card-title"><i class="bi bi-file-earmark-excel"></i> Empty...</h5>
                                                        <p class="card-text">No preadvised tenders are matching this tender desk.</p>
                                                    </div>
                                                </div>
                                            </div>
                                        <% } else { %> 
                                        <% for (entry of preadvised_inEU) { %>
                                            <div class="col col-12 col-sm-4 col-xl-3">
                                                <div class="card h-100 <% if (((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) < 0) && ((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) >= -10)) { %> 
                                                border-secondary
                                                <% } else if (((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) >= 0) && (new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) <= 10) { %> 
                                                    border-danger
                                                    <% } %>">
                                                    <% if ((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) <= 0 && (new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24 >= -10)) { %> 
                                                        <span class="position-absolute top-0 start-100 translate-middle badge bg-secondary fs-6"><i class="bi bi-alarm"></i></span>
                                                    <% } else if ((new Date(entry.expectedReceiveDate).getTime() - new Date().getTime())/(1000 * 60 * 60 * 24) <= 10) { %> 
                                                        <span class="position-absolute top-0 start-100 translate-middle badge bg-danger fs-6"><i class="bi bi-alarm"></i></span>
                                                    <% } %>
                                                    <h5 class="card-header text-truncate"><%= entry.companyName %></h5>
                                                        <div class="card-body">
                                                            <h6 class="card-title mb-0">Launch expected: <% if(entry.expectedReceiveDate.getDate() < 10) { %><%=`0${entry.expectedReceiveDate.getDate()}`%><% } else { %><%= `${entry.expectedReceiveDate.getDate()}` %><% } %>-<%= `${monthsData[entry.expectedReceiveDate.getMonth() + 1]}-${entry.expectedReceiveDate.getFullYear()}`%></h6>
                                                            <p class="card-text mt-0">From <% for (let country of countriesData) { if (country.cca2 === entry.countryLocation) { %><%= country.name.common %> <%= country.flag %><% } %><% } %></p>
                                                            <p class="card-text mb-0"><% if (entry.history.includes("historyNone")) { %><%="No history background"%><% } else if (entry.history.includes("historyAirOcean") || entry.history.includes("historyAir&Ocean")) { %><%="Existing Rhenus Air & Ocean history"%><% } else { %><%="Existing Rhenus Group history"%><% } %></p>
                                                            <% if (entry.history.includes("historyAirOcean") || entry.history.includes("historyAir&Ocean")) {%><p class="card-text mt-0"><i><%= entry.existingCustomerSegment %></i></p><%}%> 
                                                            <% if (entry.airFreightVolume) {%><p class="card-text mb-0"><%=`Air vol. ${parseInt(entry.airFreightVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} Tons`%></p><%} else if (entry.seaFreightFCLVolume) {%><p class="card-text mb-0"><%=`FCL vol. ${parseInt(entry.seaFreightFCLVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} TEUs`%></p><%} else if (entry.seaFreightLCLVolume) {%><p class="card-text mb-0"><%=`LCL vol. ${parseInt(entry.seaFreightLCLVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} CBMs`%></p><%} else if (entry.railFreightVolume) {%><p class="card-text mb-0"><%=`Rail vol. ${parseInt(entry.railFreightVolume).toLocaleString('en-US', {minimumFractionDigits: 2,maximumFractionDigits: 2})} TEUs`%></p><% } %>
                                                            <a href="/moonshot/preadvised/<%= entry._id %> " class="card-link btn btn-light btn-sm border mt-3">Details</a>
                                                        </div>
                                                </div>
                                            </div>
                                        <% } %>
                                    <% } %> 
                                </div>
                            </div>
                        </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div id="displayTable" class="col col-10 me-lg-2 me-xl-5 mb-5">
            <table class="table">
                <thead>
                  <tr>
                    <th scope="col">#</th>
                    <th scope="col">First</th>
                    <th scope="col">Last</th>
                    <th scope="col">Handle</th>
                  </tr>
                </thead>
                <tbody>
                  <tr>
                    <th scope="row">1</th>
                    <td>Mark</td>
                    <td>Otto</td>
                    <td>@mdo</td>
                  </tr>
                  <tr>
                    <th scope="row">2</th>
                    <td>Jacob</td>
                    <td>Thornton</td>
                    <td>@fat</td>
                  </tr>
                  <tr>
                    <th scope="row">3</th>
                    <td colspan="2">Larry the Bird</td>
                    <td>@twitter</td>
                  </tr>
                </tbody>
              </table>
        </div>
    </div>

    <script src="/js/preadvised_index.js"></script>
    <%- include("partials/commonBootstrap.ejs") %>
</body>
</html>